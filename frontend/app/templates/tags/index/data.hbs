<div data-test-tags>
  {{#each this.sortedTags as |tag|}}
    <LinkTo
      @route="tags.show"
      @model={{tag.name}}
      data-test-tag={{tag.name}}
      @classNames="btn btn-primary btn-large mr-1 mb-3"
    >
      {{ tag.name }}
    </LinkTo>
  {{/each}}
</div>
